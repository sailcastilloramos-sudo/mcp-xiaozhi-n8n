# docker-compose.yml
version: '3.8'

services:
  mcp-bridge:
    build: .
    container_name: mcp-xiaozhi-n8n
    restart: unless-stopped
    environment:
      - MCP_ENDPOINT=wss://api.xiaozhi.me/mcp/?token=eyJhbGciOiJFUzI1NiIsInR5cCI6IkpXVCJ9...Y689FTl1YeUbUg
      - N8N_WEBHOOK_URL=https://ser2n8n.grupohsm.net/webhook-test/xiaozhi-action
    # Si necesitas conectarte a redes específicas de tu host:
    # network_mode: "host"